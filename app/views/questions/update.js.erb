document.querySelector(".question-errors")
    .insertAdjacentHTML('afterbegin', '<%= render 'shared/errors', resource: question %>');
<% unless question.errors.present? %>
  document.querySelector(".question").innerHTML = '<%= j render question %>'
<% end %>


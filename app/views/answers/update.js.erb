document.querySelector(".answer-errors")
    .insertAdjacentHTML('afterbegin', '<%= render 'shared/errors', resource: answer %>');
<% unless answer.errors.present? %>
  document.querySelector(".answers").innerHTML = '<%= j render @question.answers_by_best %>'
<% end %>

